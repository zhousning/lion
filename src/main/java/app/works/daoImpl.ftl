package app.daos.Impl;

import org.springframework.stereotype.Repository;

import app.daos.${model.name}Dao;
import app.models.${model.name};

@Repository
public class ${model.name}DaoImpl extends BaseDaoImpl<${model.name}> implements ${model.name}Dao{

}